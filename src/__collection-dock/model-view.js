/**
 * Defines a basic view to be used for each model.
 *
 * @module collection-dock
 * @submodule model-view
 */
define(function (require, exports, module) {
	'use strict';

	var _ = require('lodash'),
		modelView = require('model-dock');

	module.exports = modelView.extend({
		initialize: function initializeModelView(options) {



		},

	})
});
